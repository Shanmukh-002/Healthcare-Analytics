# Data Model / Schema

This project uses a simple star-ish schema: two dimensions (`dim_patient`, `dim_department`) and one fact table (`fact_visits`).

![Healthcare analytics data model](data_model.svg)

